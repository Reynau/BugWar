{
  "name": "bugwar",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "browserify src/index.js -o docs/bundle.js && echo 'built!'",
    "start_server": "node src/Server/server.js",
    "start": "npm run build && npm run start_server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^16.2.2"
  },
  "dependencies": {
    "socket.io": "^2.1.0"
  }
}
