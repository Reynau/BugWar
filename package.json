{
  "name": "bugwar",
  "version": "1.0.0",
  "description": "",
  "main": "src/Server/server.js",
  "scripts": {
    "build": "browserify src/client/index.js -o docs/bundle.js && echo 'built!'",
    "start_server": "node src/Server/Server.js",
    "start": "npm run build && npm run start_server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^16.2.3"
  },
  "dependencies": {
    "socket.io": "^2.1.1"
  }
}
